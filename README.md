# CasualDeveloper Homebrew Tap

Homebrew formulas for CasualDeveloper tools.

## Install pinentry-companion

```sh
brew tap CasualDeveloper/tap
brew install pinentry-companion
pinentry-companion setup
pinentry-companion doctor
```

For the latest `main` build:

```sh
brew install --HEAD pinentry-companion
```
